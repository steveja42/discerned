# SQLite database + WAL files created by nostr-rs-relay at runtime.
data/

# Container config (0.0.0.0:8080) generated by run.ps1 from config.toml.
config.container.toml

# Temp launcher script written by run.ps1 to open the relay in its own window.
relay-window.ps1
