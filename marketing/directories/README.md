# Directory listings

Submissions to third-party app directories. Each file is the entry **as submitted** — keep it
in sync when the listing changes upstream, so this stays the source of truth rather than a
snapshot of what was once submitted.

| File | Directory | Submitted via | Status |
|---|---|---|---|
| `nostrapps.toml` | [nostrapps.com](https://nostrapps.com) | PR to `apps.toml` in the site's repo | **submitted** |
| `awesome-nostr-nostr.net.md` | [awesome-nostr](https://github.com/aljazceru/awesome-nostr) → [nostr.net](https://nostr.net) | PR adding a README line | **listed** — [PR #723](https://github.com/aljazceru/awesome-nostr/pull/723) merged |

## NIP-89 (kind 31990) — deliberately NOT published

Some directories (NostrHub) have no submission form and list apps from their NIP-89 handler
announcements. **Discerned should not publish one.** A kind 31990 advertises "send me any
naddr/nevent of these kinds and I will display it", and Discerned cannot: the feed filter is
`{ kinds: [1, 30023], '#t': ['discerned'] }` (`discerned-web/lib/nostr/feed.ts`) and the app
has no naddr/nevent route at all. It displays its own tagged events, not arbitrary ones.
Publishing a handler would send users to a page that ignores what they clicked.

Revisit only if a route that renders an arbitrary event is added. The `client` tag Discerned
already stamps is a different, unrelated part of NIP-89.

## Maintenance notes

**Gallery and thumb URLs both point at `discerned.online`** — our own domain, so they survive
repo renames, branch renames, and a move off GitHub. The gallery images are the Chrome Web
Store screenshots, stored once at `discerned-web/public/press/` and served from there; they are
deliberately NOT duplicated under `marketing/`. `thumb` uses `icons/icon128.png`, generated by
`pnpm gen:icons` and committed.

The Web Store's own CDN links (`lh3.googleusercontent.com`) were used originally and should not
be reinstated: they are opaque, unversioned, and re-uploading a screenshot in the dashboard
mints a new URL, silently breaking the gallery.

**Changing a screenshot** means replacing the file in `discerned-web/public/press/` — the URL
stays the same, so no directory resubmission is needed. Note this lives under `discerned-web/`,
so such a change DOES trigger a Netlify deploy (by design: the file has to ship to be served).
Re-upload the same image to the Web Store dashboard separately; the two are not linked.

**Feature-list claims must match shipped behaviour.** See `../CLAUDE.md` for the terminology
rules. The two easiest to get wrong here: private clips are local-only and *not* encrypted
(NIP-44 is stubbed), and casting is capture-time only — a stored clip cannot be cast later, so
never phrase a feature as though it can.
